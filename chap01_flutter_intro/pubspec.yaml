# 이 파일이 변경되면 반드시 Pub get 을 눌러서 적용 !
name: chap01_flutter_intro
description: "A new Flutter project."
# 간단한 설명

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/
#    이 이미지의 하위에 있는 내용을 다 가지고 오게 할거라는 의미
